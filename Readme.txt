Import shootball.sql via phpmyadmin or sqlbuddy

Clone to a folder "myShootball" in your localhost root and run localhost/myShootball from the browser.

Make sure that in apache settings, module mod_rewrite is enabled. 